.payment-method-icon {
  width: 64px;
  height: 64px;
  object-fit: contain;
  transition: transform 0.3s ease;
}

.payment-method-icon:hover {
  transform: scale(1.1);
}

/* Responsive sizes */
@media (max-width: 768px) {
  .payment-method-icon {
    width: 48px;
    height: 48px;
  }
}